# Leave empty in dev to use Vite proxy (same origin → /api → backend)
# Or set full URL for direct calls, e.g. https://api.yourapp.com
VITE_API_URL=

# Dev only: backend URL for Vite /api proxy + Socket.io (must match backend PORT). Default port 5050
# VITE_DEV_API_ORIGIN=http://localhost:5050
