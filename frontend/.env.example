# Production / remote API (Render). Trailing slash optional.
VITE_API_URL=https://stream-sence.onrender.com

# Local dev: comment out VITE_API_URL above and use Vite proxy instead, or set:
# VITE_API_URL=
# VITE_DEV_API_ORIGIN=http://localhost:5050
